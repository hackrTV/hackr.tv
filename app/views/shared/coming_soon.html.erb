<% title = @preview_title || "THE PULSE GRID" %>
<% content_for :title, "#{title} — hackr.tv" %>

<%# Admin-preview coming-soon page (AdminPreview concern) — rendered in
    place for non-admins while a surface is gated ahead of public
    rollout. Reuses the grid coming-soon styling (grid_game.css). %>
<div class="grid-coming-soon">
  <div class="grid-coming-soon-frame">╔══════════════════════════════════════╗</div>
  <h1 class="grid-coming-soon-title"><%= title %></h1>
  <p class="grid-coming-soon-lede">will open soon</p>
  <p class="grid-coming-soon-sub">// access pending</p>
  <div class="grid-coming-soon-frame">╚══════════════════════════════════════╝</div>
  <a href="/" class="grid-coming-soon-link">return to hackr.tv</a>
</div>
