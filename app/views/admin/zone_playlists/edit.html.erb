<div class="tui-window cyan-168 white-text" style="display: block; max-width: 900px; margin: 30px auto; padding: 20px;">
  <fieldset class="cyan-168-border">
    <legend class="center">/root/zone_playlists/<%= @zone_playlist.id %>/edit :: EDIT PLAYLIST</legend>

    <div style="padding: 20px;">
      <div style="margin-bottom: 20px;">
        <%= link_to "← Back to Playlist", admin_zone_playlist_path(@zone_playlist), class: "tui-button green-168", style: "padding: 8px 20px;" %>
      </div>

      <%= render "form", zone_playlist: @zone_playlist %>
    </div>
  </fieldset>
</div>
