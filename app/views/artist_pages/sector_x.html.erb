<%# ERB port of SectorXPage.tsx (GET /sector/x). %>
<% content_for :title, "SECTOR X — hackr.tv" %>

<%= render layout: "shared/default_chrome" do %>
  <div class="tui-window white-text sectorx-window">
    <fieldset class="sectorx-fieldset">
      <legend class="center sectorx-legend">SECTOR X</legend>
      <div class="sectorx-body">
        <p class="sectorx-coming-soon">[:: COMING SOON ::]</p>
        <p class="sectorx-tagline">Keep your hackr eyes peeled!</p>
      </div>
    </fieldset>
  </div>
<% end %>
