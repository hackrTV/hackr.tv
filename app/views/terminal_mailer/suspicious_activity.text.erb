TERMINAL AUDIT — SUSPICIOUS ACTIVITY
=====================================

<%= @flags.size %> flag<%= @flags.size == 1 ? '' : 's' %> detected since <%= @since.strftime('%Y-%m-%d %H:%M UTC') %>

<% @flags.each do |flag| %>
  <%= flag %>
<% end %>

--
Generated: <%= @generated_at.strftime('%Y-%m-%d %H:%M UTC') %>
hackr.tv // TERMINAL AUDIT
